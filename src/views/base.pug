doctype html 
html(lang="ko")
    head 
        title #{pageTitle} | Moontube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")

    body
        header 
            nav 
                ul
                    li 
                        a(href="/") Home
                    
                    li 
                        a(href="/search") Search

                    if loggedIn
                        li
                            a(href="/users/profile") #{loggedInUser.name}의 profile
                   
                        li 
                            a(href="/users/logout") Logout
                        li
                            a(href="/videos/upload") Upload Video
                    else
                    
                        li 
                            a(href="/login") Login
                        li 
                            a(href="/join") Join
                   
                   
            h1=pageTitle
        main 
            block content
    include partials/footer.pug